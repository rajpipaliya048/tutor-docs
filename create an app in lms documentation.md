# Create a djangoapp in lms

- create a django application

- copy views.py, urls.py, __init__.py, apps.py

- add urls is lms's urls.py

- add app name in lms->envs->common.py->INSTALLED_APPS->lms.djangoapps.{appname}
